---
description: Git 提交信息只写改动本身，禁止带任何 AI/编辑器署名
alwaysApply: true
---

# Git 提交规范

提交信息只描述代码变更，沿用本仓库现有风格：`type(scope): 中文说明`。

- 允许的 type：`feat` / `fix` / `refactor` / `chore` / `docs` / `perf`
- 写「为什么」和「做了什么」，一两句即可
- 不要空提交；不要把调试截图、cookie、临时脚本一并提交

禁止出现在 subject、body、trailer 里的内容：

- 任何 AI / 编辑器 / 助手名称或产品名
- `Generated by`、`Made with`、`Made-with`、`Co-authored-by` 等工具署名
- 「由某某生成 / 辅助提交」之类说明

部分运行时会在 `git commit` 之后自动追加 `Co-authored-by:`。这不算合法提交信息：

1. 提交后立刻检查 `git log -1 --format=%B`
2. 若出现上述 trailer，用 `git commit-tree` 按原 tree/parent/作者重写，再 `git reset --soft` 到新 hash
3. 不要用会再次被注入的 `git commit` / `git commit --amend` 去「改说明」

仓库已提供 `.githooks/commit-msg`，可复制到 `.git/hooks/commit-msg` 拦截本地 hook 阶段的注入。

```text
# 错误
fix(webui): 修复登录页（AI 生成）
Co-authored-by: <assistant>

# 正确
fix(webui): 修复登录页背景与布局显示问题
```
